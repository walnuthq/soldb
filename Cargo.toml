[workspace]
members = [
    "crates/soldb-bridge",
    "crates/soldb-cli",
    "crates/soldb-compiler",
    "crates/soldb-core",
    "crates/soldb-debugger",
    "crates/soldb-dap",
    "crates/soldb-ethdebug",
    "crates/soldb-evm",
    "crates/soldb-profiler",
    "crates/soldb-repl",
    "crates/soldb-rpc",
    "crates/soldb-serializer",
    "crates/soldb-wasm",
]
resolver = "2"

[workspace.package]
version = "0.2.0"
edition = "2021"
license = "GPL-3.0-only OR MIT"
repository = "https://github.com/walnuthq/soldb"
homepage = "https://github.com/walnuthq/soldb"
keywords = ["solidity", "evm", "ethereum", "debugger", "ethdebug"]
categories = ["development-tools::debugging"]
readme = "README.md"

[workspace.dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1", features = ["derive", "rc"] }
serde_json = "1"
thiserror = "2"
soldb-bridge = { path = "crates/soldb-bridge", version = "0.2.0" }
soldb-compiler = { path = "crates/soldb-compiler", version = "0.2.0" }
soldb-core = { path = "crates/soldb-core", version = "0.2.0" }
soldb-dap = { path = "crates/soldb-dap", version = "0.2.0" }
soldb-debugger = { path = "crates/soldb-debugger", version = "0.2.0" }
soldb-ethdebug = { path = "crates/soldb-ethdebug", version = "0.2.0" }
# Off by default here so a crate that wants the transport without REVM can say so; the
# `replay` feature of each dependent turns it back on.
soldb-evm = { path = "crates/soldb-evm", version = "0.2.0", default-features = false }
soldb-profiler = { path = "crates/soldb-profiler", version = "0.2.0" }
soldb-repl = { path = "crates/soldb-repl", version = "0.2.0" }
soldb-rpc = { path = "crates/soldb-rpc", version = "0.2.0" }
soldb-serializer = { path = "crates/soldb-serializer", version = "0.2.0" }

[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.lints.clippy]
all = { level = "warn", priority = -1 }
module_name_repetitions = "allow"
